#pragma once

/*
print the multiplication table from start_value to end_value

@param startValue: the smallest multiplication value in the table
@param endValue: the biggest multiplication value in the table
*/
void printMultiplyTable(int startValue, int endValue);

/*
print padding spaces to have fixed print size to every num

@param highestSpaceNumber: the fixed space to every number(including spaces)
@param result: the number you want to pad spaces in print
*/
void printPaddingSpaces(int highestSpcaceNumber, int result);